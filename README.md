# quo-server
a grpc api server for quotes

Uses task files, install task with 
```
go install github.com/go-task/task/v3/cmd/task@latest
```

Uses the proto defined at `https://github.com/Rosalita/quo-proto/proto`

